// import React, { Component } from 'react'

// const SortedIssues = ({ sortedIssuesFunc }) => {

//     return (
//         // pass the sortedIssuesFunc from Dashboard and get the event by onChange value (option from select. date or priority). will sort by option in Dashboard. 
//         <select onChange={(e) => sortedIssuesFunc(e.target.value)}>
//             <option  selected>filter by ...</option>
//             <option value={"date"}>by date</option>
//             <option value={"priority"}>by priority</option>
//         </select>
//     )

// }

// export default SortedIssues;
